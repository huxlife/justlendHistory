{
  "name": "justlend-liquidation",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"node server.js\" \"npm start --prefix frontend\"",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
